{
  "name": "@dorgtech/web3modal-ts",
  "version": "0.0.1-alpha",
  "description": "Web3modal's typescript-only module",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "package:release": "rm -rf ./package && mkdir ./package && cp -r ./dist/ ./package/dist && cp README.md package.json ./package",
    "release": "npm run build && npm run package:release && npm publish ./package --access public"
  },
  "main": "dist/index.js",
  "devDependencies": {
    "@types/node": "^14.11.8",
    "isomorphic-fetch": "^2.2.1",
    "typescript": "^3.1.3"
  },
  "dependencies": {
    "detect-browser": "^5.1.0",
    "tslib": "^1.10.0"
  }
}
